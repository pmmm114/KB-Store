# KB-Store Agent 작업 지침

## 🤖 Agent 역할 정의
Claude Code Agent는 KB-Store 모노레포에서 기획 명세를 기반으로 기술적 구현을 담당합니다.

## 📋 문서 구조 이해

### Option B 문서 체계
- **CLAUDE.md**: 기획자용 비즈니스 명세 (기획 의도, 사용자 여정, 비즈니스 로직)
- **AGENT-GUIDE.md**: Agent용 기술 지침 (현재 파일, 구현 방법, 아키텍처 제약)
- **docs/**: 상세 기술 문서 (API 스펙, 컴포넌트 명세, 테스트 전략)

### 작업 우선순위
1. **비즈니스 이해 우선**: 항상 CLAUDE.md의 기획 의도를 먼저 파악
2. **기술적 구현**: AGENT-GUIDE.md의 지침을 따라 구현
3. **세부 사항**: docs/ 폴더의 상세 명세 참조

## 🎯 전역 Agent 규칙

### Documentation-Driven Development (DDD)

#### 작업 전 (Pre-work)
1. **문서 탐색 순서**:
   - 현재 작업 디렉토리의 `CLAUDE.md` (비즈니스 이해)
   - 현재 작업 디렉토리의 `AGENT-GUIDE.md` (기술 지침)
   - 관련 `docs/` 폴더의 상세 명세
   - 상위 디렉토리의 CLAUDE.md, AGENT-GUIDE.md (순서대로)

2. **계획 수립**:
   - TodoWrite로 작업 계획 수립
   - 비즈니스 요구사항과 기술적 제약사항 균형 고려
   - 병렬 처리 가능한 작업 식별

#### 작업 중 (During-work)
1. **진행 상황 추적**: TodoWrite로 실시간 업데이트
2. **기획 의도 준수**: 구현 중 기획 의도에서 벗어나지 않는지 지속 확인
3. **문서 동기화**: 주요 구현 단계마다 관련 문서 업데이트

#### 작업 후 (Post-work)
1. **문서 업데이트**: 구현 내용을 문서에 반영
2. **품질 검증**: lint, typecheck 실행 및 오류 수정
3. **명세 최신화**: 변경사항을 적절한 문서 레벨에 반영

### 계층적 규칙 적용

#### 우선순위 체계
1. **가장 가까운 AGENT-GUIDE.md** (최우선)
2. **가장 가까운 CLAUDE.md** (비즈니스 컨텍스트)
3. **상위 AGENT-GUIDE.md들** (기술적 제약사항 상속)
4. **상위 CLAUDE.md들** (비즈니스 컨텍스트 상속)
5. **Claude Code 시스템 기본 규칙**

#### 충돌 해결
- 기술적 제약과 비즈니스 요구사항이 충돌할 경우 비즈니스 우선
- 동일 레벨에서 CLAUDE.md와 AGENT-GUIDE.md가 충돌할 경우 CLAUDE.md 우선
- 불명확한 경우 상위 레벨 문서에서 지침 확인

## 🔧 기술적 작업 원칙

### 코드 작성 규칙
- **중요 맥락 주석**: 비즈니스 로직이나 복잡한 알고리즘의 핵심 맥락 설명
- **의도 명확화**: 코드의 '왜'를 설명하는 주석 작성 권장
- **TODO/FIXME**: 임시 해결책이나 향후 개선 사항 표시

### 품질 관리
- **기존 컨벤션 준수**: 각 앱의 기존 코드 스타일 및 패턴 따르기
- **라이브러리 확인**: 사용 전 해당 앱에서 사용 중인 라이브러리 확인
- **타입 안정성**: TypeScript 프로젝트의 경우 엄격한 타입 정의

### 문서화 규칙
- **Mermaid 다이어그램 필수**: 모든 플로우차트, 시퀀스 다이어그램은 Mermaid 사용
- **다이어그램 유형**:
  - 플로우차트: 사용자 플로우, 시스템 플로우
  - 시퀀스 다이어그램: API 호출 플로우, 컴포넌트 간 상호작용
  - 상태 다이어그램: 상태 전이, 라이프사이클
  - 클래스 다이어그램: 데이터 모델, 인터페이스 구조

### 병렬 작업 관리
- **다중 작업 처리**: 여러 작업을 병렬적으로 처리하여 개발 효율성 극대화
- **Tool 병렬 실행**: 단일 메시지에서 여러 도구를 동시 실행
- **Agent 병렬 실행**: 복잡한 작업 시 여러 전문 agent 동시 활용
- **동기화 지점**: 병렬 작업 완료 후 결과 통합 지점 명시

### TodoWrite 활용 강화
- **병렬 작업 식별**: 동시 처리 가능한 작업들 구분
- **병렬 그룹 표시**: 동시 실행 가능한 작업들 그룹화
- **의존성 명시**: 작업 간 의존성이 있는 경우 순서 표시
- **문서 작업 포함**: 각 단계별 문서 작업 명시
- **PR 생성 준비**: 작업 완료 후 PR 생성 단계 포함

## 🚀 PR 자동화 프로세스
- **자동 PR 생성**: 작업 완료 후 gh CLI를 활용한 자동 PR 생성
- **병렬 작업 결과 통합**: 여러 병렬 작업 결과를 논리적 단위로 통합하여 PR 생성
- **문서 동기화 확인**: PR에 관련 문서 업데이트 포함 여부 검증
- **자동 검증**: lint, typecheck, 테스트 통과 후 PR 생성

## 📂 앱별 특화 지침
각 앱 디렉토리의 `AGENT-GUIDE.md`에서 해당 앱에 특화된 기술 지침을 확인하세요:

- `apps/tarote/AGENT-GUIDE.md`: 타로 리딩 앱 기술 지침
- `apps/other-app/AGENT-GUIDE.md`: 기타 앱 기술 지침

## 🔧 개발 환경 설정
- **모노레포 도구**: NX
- **패키지 매니저**: pnpm
- **Git 브랜치**: feature/ 브랜치에서 main으로 PR
- **GitHub CLI**: gh 명령어로 PR 관리

## ⚠️ 주의사항
- **비즈니스 우선**: 기술적 편의성보다 비즈니스 요구사항 우선 고려
- **문서 일관성**: 구현 후 반드시 관련 문서들의 일관성 확인
- **보안 원칙**: 방어적 보안 작업만 지원, 악의적 코드 생성/개선 거부

---

> 이 지침은 Claude Code Agent가 KB-Store 프로젝트에서 작업할 때 따라야 하는 기술적 규칙입니다.
> 비즈니스 맥락은 각 레벨의 CLAUDE.md를 참조하세요.